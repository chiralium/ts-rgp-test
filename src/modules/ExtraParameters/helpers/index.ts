const livingPower = (power: number): number => power + 3;
const evasion = (dexterity: number): number => dexterity + 10;
const energy = (dexterity: number, iq: number): number => dexterity + iq;

export {
    livingPower,
    evasion,
    energy,
};
